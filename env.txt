ethaddr=fc:0f:e7:14:27:f2
serverip=10.16.18.12
bootdelay=5
autostart=n
autoload=n
memaddr=30000000
elffile=psc/psc.elf
bitfile=psc/psc.bit
bootcmd=setenv autostart n\
  dhcp\
  tftpboot 0x${memaddr} ${bitfile}\
  fpga loadb 0 0x${memaddr} 0x${filesize}\
  tftpboot 0x${memaddr} ${elffile}\
  setenv autostart y\
  bootelf 0x${memaddr}
